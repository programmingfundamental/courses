# Analysis fixture. TODO: add quality gates and immutable artifact deployment.
name: incomplete-ai-ci
on: [push]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install scikit-learn fastapi
      - run: echo "TODO tests; this is not a quality gate"
