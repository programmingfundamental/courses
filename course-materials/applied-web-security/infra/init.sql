CREATE USER lab_runtime PASSWORD 'LocalLabRuntime-2026';
CREATE TABLE app_users (username VARCHAR(64) PRIMARY KEY, password VARCHAR(100) NOT NULL, role VARCHAR(16) NOT NULL, display_name VARCHAR(200) NOT NULL, sensitive VARCHAR(2048));
CREATE TABLE documents (id BIGINT PRIMARY KEY, owner VARCHAR(64) NOT NULL, title VARCHAR(200) NOT NULL);
CREATE TABLE comments (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, author VARCHAR(64) NOT NULL, body VARCHAR(1000) NOT NULL);
GRANT CONNECT ON DATABASE lab TO lab_runtime;
GRANT USAGE ON SCHEMA public TO lab_runtime;
GRANT SELECT,INSERT,UPDATE,DELETE ON ALL TABLES IN SCHEMA public TO lab_runtime;
GRANT USAGE,SELECT ON ALL SEQUENCES IN SCHEMA public TO lab_runtime;
REVOKE CREATE ON SCHEMA public FROM PUBLIC;
